// import { api as index } from "..";

// const api = index.injectEndpoints({
// endpoints: (build) => ({
// getCreadits: build.query<CREADITS.GetResponse, CREADITS.GetRequest>({
//   query: (query) => ({
//     url: `/${query}/popular`,
//     method: "GET",
//   }),
//   providesTags: ["creadits"],
// }),
// }),
// });

// export const { useGetCreaditsQuery } = api;
